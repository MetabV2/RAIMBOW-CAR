while wait(0.1) do
	local r = math.random(1,255)
	local g = math.random(1,255)
	local b = math.random(1,255)
game:GetService("ReplicatedStorage").RemoteEvents.PlayersCar:FireServer("PickingCarColor",Color3.new(r, g, b))
end
